apiVersion: v1
kind: Pod
metadata:
  labels:
    app: myapp
    env: dev
  name: mypod
  annotations:
     describe: test pod created 
spec:
  containers:
  - image: nginx:latest
    name: con1 
    ports:
    - containerPort: 80
